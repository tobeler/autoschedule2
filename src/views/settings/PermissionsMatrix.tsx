// STUB — phase7-settings agent will replace with the permissions matrix.
export function PermissionsMatrix() {
  return (
    <div className="empty">
      <div className="h4">Permissions — building Phase 7</div>
      <div className="muted small">Role × permission grid (Dispatcher, Manager, Tech, FSM, Admin).</div>
    </div>
  );
}
