import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 7: Templates / Rules / Integrations (HubSpot) / Roles / Hours / Permissions / Forms. */
export function SettingsView() {
  return (
    <div className="view-stub">
      <EmptyState
        title="Settings — building Phase 7"
        body="Job templates, business rules, HubSpot field mapper (with Jetson real fields), roles, hours, permissions, completion forms."
      />
    </div>
  );
}
