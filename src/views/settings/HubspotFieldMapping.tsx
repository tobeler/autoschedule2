// STUB — phase7-settings agent will replace with the full field-mapper UI
// (entity tabs Customer↔Contact / Project↔Deal / Job→Job custom object /
// Service Area↔Service Area, drop-down field pickers both sides, direction
// toggle, type badges, required-field guard, duplicate-prevention,
// pre-populated from DEFAULT_HUBSPOT_MAPPINGS).
export function HubspotFieldMapping() {
  return (
    <div className="muted small" style={{ padding: 16 }}>
      HubSpot field mapping UI — building Phase 7.
    </div>
  );
}
