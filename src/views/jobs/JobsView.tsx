import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 6: sortable + filterable Jobs table. */
export function JobsView() {
  return (
    <div className="view-stub">
      <EmptyState title="Jobs table — building Phase 6" body="Type chips, status filters, search, crew composition column." />
    </div>
  );
}
