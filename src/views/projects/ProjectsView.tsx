import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 6: Projects list + detail drawer (multi-day grouping). */
export function ProjectsView() {
  return (
    <div className="view-stub">
      <EmptyState title="Projects — building Phase 6" body="HubSpot-synced project layer between customers and jobs." />
    </div>
  );
}
