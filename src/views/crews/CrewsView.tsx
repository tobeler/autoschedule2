import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 6: Default crews + This-week composition + skills matrix. */
export function CrewsView() {
  return (
    <div className="view-stub">
      <EmptyState title="Crews — building Phase 6" body="Default-crew rosters, weekly composition with loan flags, skills matrix." />
    </div>
  );
}
