import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 8: Crew utilization, first-time-fix, drive-time saved. */
export function ReportsView() {
  return (
    <div className="view-stub">
      <EmptyState title="Reports — building Phase 8" body="Crew utilization, first-time-fix rate, revenue per truck, drive-time saved by routing." />
    </div>
  );
}
