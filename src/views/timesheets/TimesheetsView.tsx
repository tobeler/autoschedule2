import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 6: Auto-derived timesheets with approve flow + OT flags. */
export function TimesheetsView() {
  return (
    <div className="view-stub">
      <EmptyState title="Timesheets — building Phase 6" body="Week grid auto-derived from job assignments. Approve / OT flags / draft → pending → approved." />
    </div>
  );
}
