import { EmptyState } from '../../components/EmptyState';

/** STUB — Phase 6: Truck/van fleet table with utilization bars. */
export function FleetView() {
  return (
    <div className="view-stub">
      <EmptyState title="Fleet — building Phase 6" body="Trucks and vans with utilization, today's jobs, in-shop/available status." />
    </div>
  );
}
